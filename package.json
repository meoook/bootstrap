{
  "name": "bootstrap",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.1",
    "@bit/bazha.easyui.loader": "file:./src/bit/loader"
  },
  "devDependencies": {
    "node-sass": "^4.14.1",
    "typescript": "^4.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "bit": {
    "env": {
      "compiler": "bit.envs/compilers/react"
    },
    "componentsDefaultDirectory": "src/bit/{name}",
    "packageManager": "npm"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
